{
  "frontend/src/App.tsx": "Main app with TanStack Router, QueryClient, and dark theme — renders the sidebar+outlet layout directly on mount without any authentication gate, login page, or profile setup modal.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — removed the `useDeleteTransaction` hook that referenced the non-existent `actor.deleteTransaction` method; all other hooks for inventory, transactions, shop settings, reports, work orders, and user profiles remain intact.",
  "frontend/src/components/Sidebar.tsx": "Static dark navy sidebar with navigation links only (Kasir, Inventori, Servis, Transaksi, Laporan, Pengaturan) — removed user profile display, logout button, and all authentication-related imports and logic.",
  "frontend/src/components/Receipt.tsx": "Print-friendly receipt component in Indonesian — added display of `customerPhone` (Nomor HP) between the customer name and vehicle info rows, shown only when the value is non-empty.",
  "frontend/src/lib/utils.ts": "Utility functions: cn (class merging), formatRupiah (IDR currency), formatDateTime/formatDate (Indonesian locale), and timestamp conversion helpers.",
  "frontend/src/pages/CashierPage.tsx": "POS cashier page with product/service grid, cart management, customer info fields with work order auto-fill from sessionStorage, and checkout flow — uses correct hook names `useInventoryItems` and `useShopSettings`.",
  "frontend/src/pages/CatalogPage.tsx": "Catalog management page with tabbed Products/Services views, add/edit/delete dialogs, stock badges, and search filtering.",
  "frontend/src/pages/TransactionHistoryPage.tsx": "Transaction history page in Indonesian — removed the delete transaction button, delete confirmation dialog, and related state/imports since the backend does not support deleting transactions; retains search/date filtering, detail view dialog, and print functionality.",
  "frontend/src/pages/ReportsPage.tsx": "Reports page with daily/monthly/top-selling/profit-loss tabs in Indonesian — fixed to use correct hook names (`useDailyReport`, `useMonthlyReport`, `useTopSellingItems`) and converts date strings to `bigint` nanoseconds before passing to `useProfitLoss`.",
  "frontend/src/pages/SettingsPage.tsx": "Shop settings page fully in Indonesian — logo upload, shop info form fields, buttons, progress messages, and toast notifications all use Indonesian text.",
  "frontend/src/index.css": "OKLCH design system with dark navy sidebar, orange-amber brand color, Inter/Roboto Condensed fonts, and comprehensive @media print rules for receipt-only printing.",
  "frontend/tailwind.config.js": "Extended Tailwind config with brand color tokens, Inter/Roboto Condensed font families, and custom shadow utilities.",
  "frontend/src/pages/InventoryPage.tsx": "Inventory management page with tabbed goods/services view, stats cards, add/edit-stock/delete dialogs, and async Excel import/export — uses correct hook name `useInventoryItems` and properly awaits `buildXlsx` which returns a Promise.",
  "frontend/src/lib/xlsx.ts": "Self-contained XLSX writer and reader using raw DEFLATE via CompressionStream — fixed the `readXlsx` function to correctly place each cell at its proper column index using the cell reference attribute (e.g. \"C5\"), handle rich-text `<r><t>` runs in shared strings, and support inline strings (`t=\"inlineStr\"`), ensuring all rows added below existing data in Excel are correctly parsed regardless of empty cells or column gaps.",
  "frontend/src/pages/ServicePage.tsx": "Work order management page with create/view/complete/delete dialogs, status tracking, search filtering, and navigation to cashier — uses correct hook name `useWorkOrders`, navigates to `'/'`, and passes `bigint` directly to `formatDate` without wrapping in `Number()`.",
  "frontend/src/pages/LoginPage.tsx": "Standalone login page with Internet Identity login button, Bengkel POS branding, and caffeine.ai footer attribution.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog that prompts new authenticated users to enter their name and save their profile via the backend.",
  "frontend/src/lib/urlParams.ts": "Utility module for passing work order data between pages via sessionStorage — provides storeWorkOrderNavData, readWorkOrderNavData, and clearWorkOrderNavData functions."
}